package com.tourtrek;

public class AddAttractionFragmentUnitTest {

    // TODO when creating an attraction for an existing tour from the personal tours tab, the attraction is reflected in the database as a reference
    // within a tour

    // TODO when creating an attraction for a new tour from the add tour user story, the attraction is reflected in the database but
    // not as a reference within a tour
}
